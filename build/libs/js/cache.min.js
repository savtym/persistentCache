'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var globalSize = 5242880; // 5MB

var idMemory = 1;
var currentSize = 0;

var Cache = function () {
  _createClass(Cache, null, [{
    key: 'freeSize',
    value: function freeSize() {
      return globalSize - currentSize;
    }
  }]);

  function Cache() {
    var sizeRAM = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : 16384;
    var sizeLocal = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 16384;

    _classCallCheck(this, Cache);

    if (Cache.freeSize < sizeLocal) {
      throw 'Memory is full';
    }

    this.persistent = new Persistent(sizeRAM, sizeLocal, idMemory++);
    currentSize += this.persistent.freeMemoryLocalStorage;

    return this.persistent;
  }

  _createClass(Cache, null, [{
    key: 'clear',
    value: function clear() {
      localStorage.clear();
    }
  }]);

  return Cache;
}();

exports.default = Cache;
'use strict';

var _slicedToArray = function () { function sliceIterator(arr, i) { var _arr = []; var _n = true; var _d = false; var _e = undefined; try { for (var _i = arr[Symbol.iterator](), _s; !(_n = (_s = _i.next()).done); _n = true) { _arr.push(_s.value); if (i && _arr.length === i) break; } } catch (err) { _d = true; _e = err; } finally { try { if (!_n && _i["return"]) _i["return"](); } finally { if (_d) throw _e; } } return _arr; } return function (arr, i) { if (Array.isArray(arr)) { return arr; } else if (Symbol.iterator in Object(arr)) { return sliceIterator(arr, i); } else { throw new TypeError("Invalid attempt to destructure non-iterable instance"); } }; }();

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

//
// eventEmiter.js
//

/*
 option for cookie:
 name - name cookie
 value - value cookie
 options:
 expires - time life for coolie in seconds
 path - path for cookie
 domain - domain for cookie
 secure - true/false if need send cookie with secure canal
 */

var id = void 0;
var lifeTime = {};
var busySizeLocalStorage = 0;

var Persistent = function () {
  function Persistent(maxSizeRAM, maxSizeLocalStorage, curId) {
    _classCallCheck(this, Persistent);

    id = curId;
    this.maxSizeLocalStorage = maxSizeLocalStorage;
    this.maxSizeRAM = maxSizeRAM;
  }

  _createClass(Persistent, [{
    key: 'insertLocalStorage',
    value: function insertLocalStorage(key, data) {
      var sizeData = Persistent._roughSizeOfObject(data);
      var freeSize = this.freeMemoryLocalStorage;

      if (sizeData < freeSize) {

        this.setValues(key, data, JSON.parse(localStorage.getItem(id)) || {}, sizeData);
      } else if (sizeData < this.maxSizeLocalStorage) {

        var overwriting = Persistent.leastRecentlyUsed(lifeTime, sizeData - freeSize);
        var values = this.allValues();

        var _iteratorNormalCompletion = true;
        var _didIteratorError = false;
        var _iteratorError = undefined;

        try {
          for (var _iterator = overwriting[Symbol.iterator](), _step; !(_iteratorNormalCompletion = (_step = _iterator.next()).done); _iteratorNormalCompletion = true) {
            var _step$value = _slicedToArray(_step.value, 2),
                _key = _step$value[0],
                obj = _step$value[1];

            busySizeLocalStorage -= obj.size;
            delete values[_key];
          }
        } catch (err) {
          _didIteratorError = true;
          _iteratorError = err;
        } finally {
          try {
            if (!_iteratorNormalCompletion && _iterator.return) {
              _iterator.return();
            }
          } finally {
            if (_didIteratorError) {
              throw _iteratorError;
            }
          }
        }

        this.setValues(key, data, values, sizeData);
      } else {
        console.log('Max size localStorage is less than this data');
      }
    }
  }, {
    key: 'setValues',
    value: function setValues(key, data, values, sizeData) {
      values[key] = data;
      busySizeLocalStorage += sizeData;
      this.setKeyLifeTime(key, sizeData);

      localStorage.setItem(id, JSON.stringify(values));
    }
  }, {
    key: 'setKeyLifeTime',
    value: function setKeyLifeTime(key, sizeData) {
      lifeTime[key] = {
        size: sizeData,
        time: new Date().getTime()
      };
    }
  }, {
    key: 'allValues',
    value: function allValues() {
      return JSON.parse(localStorage.getItem(id));
    }
  }, {
    key: 'freeMemoryLocalStorage',
    get: function get() {
      return this.maxSizeLocalStorage - busySizeLocalStorage;
    }
  }], [{
    key: 'leastRecentlyUsed',
    value: function leastRecentlyUsed(lifeTime, needSize) {

      var result = [];
      var sortable = [];
      var curSizeFree = 0;

      for (var key in lifeTime) {
        sortable.push([key, lifeTime[key]]);
      }

      sortable.sort(function (a, b) {
        return a[1].time - b[1].time;
      });

      var _iteratorNormalCompletion2 = true;
      var _didIteratorError2 = false;
      var _iteratorError2 = undefined;

      try {
        for (var _iterator2 = sortable[Symbol.iterator](), _step2; !(_iteratorNormalCompletion2 = (_step2 = _iterator2.next()).done); _iteratorNormalCompletion2 = true) {
          var _step2$value = _slicedToArray(_step2.value, 2),
              _key2 = _step2$value[0],
              obj = _step2$value[1];

          result.push([_key2, obj]);
          curSizeFree += obj.size;
          if (curSizeFree >= needSize) {
            break;
          }
        }
      } catch (err) {
        _didIteratorError2 = true;
        _iteratorError2 = err;
      } finally {
        try {
          if (!_iteratorNormalCompletion2 && _iterator2.return) {
            _iterator2.return();
          }
        } finally {
          if (_didIteratorError2) {
            throw _iteratorError2;
          }
        }
      }

      return result;
    }
  }, {
    key: '_roughSizeOfObject',
    value: function _roughSizeOfObject(str) {
      return JSON.stringify(str).length * 2;
    }
  }]);

  return Persistent;
}();